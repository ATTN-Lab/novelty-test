[project]
name = "patent-novelty-mcp"
version = "0.1.0"
description = "MCP server for cross-provider patent novelty checks"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
